import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function run() { const oldId = '574c335c-7e15-4d82-b01b-7ae3661dc5ed'; const newId = '28e7b48c-7daf-475f-a80a-0a3abe889a52'; const { error: bErr } = await supabase.from('bookings').update({ room_id: newId, unlocked_bedrooms: 2 }).eq('room_id', oldId); console.log('Bookings update error:', bErr); const { error: hErr } = await supabase.from('housekeeping_tasks').update({ room_id: newId }).eq('room_id', oldId); console.log('Housekeeping update error:', hErr); const { error: mErr } = await supabase.from('maintenance_tickets').update({ room_id: newId }).eq('room_id', oldId); console.log('Maintenance update error:', mErr); const { error: delErr } = await supabase.from('rooms').delete().eq('id', oldId); console.log('Room delete error:', delErr); console.log('Migration done.'); } run();
