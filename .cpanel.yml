---
deployment:
  tasks:
    - export DEPLOYPATH=/home/sparkle7/public_html/gittest.sparklesapartments.ng
    - /bin/cp -R frontend/dist/* $DEPLOYPATH/
    - /bin/cp frontend/dist/.htaccess $DEPLOYPATH/ || true
